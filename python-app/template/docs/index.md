# Documentation of ${{values.app_name}}

The application has two endpoints:
- /api/v1/info
- /api/v1/health


# How to access

The application can be accessed by going to the following url:
    ${{values.app_name}}-${{values.app_env}}-latest.test.com/api/v1/health
    ${{values.app_name}}-${{values.app_env}}-latest.test.com/api/v1/info